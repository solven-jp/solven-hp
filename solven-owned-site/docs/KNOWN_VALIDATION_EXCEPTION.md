# 既知の検査例外

正本archive内の末尾空行は、配布ZIPとのバイト一致を優先します。一般的なwhitespace検査が指摘しても既知例外として扱い、`canonical/Solven_Business_Canonical_v1.2.0_2026-07-18/`を整形・修正しません。

この例外は正本archiveだけに限定します。owned-siteの新規実装差分は`git diff --check`を通過させます。端末固有path、repo外patch、local runtime証跡は外部共有成果物へ含めません。
